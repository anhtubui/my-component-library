// This file previously provided Emotion Global styles.
// It now re-exports a side-effect CSS import so consumers can choose to import CSS variables directly.
// Keeping the same module path to avoid breaking changes.
import "./variables.css";

export {};
